import { CategoryToStringPipe } from './category-to-string.pipe';

describe('CategoryToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
