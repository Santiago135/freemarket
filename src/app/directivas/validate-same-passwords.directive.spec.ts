import { ValidateSamePasswordsDirective } from './validate-same-passwords.directive';

describe('ValidateSamePasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSamePasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
