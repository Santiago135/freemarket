import { LogbuttonDirective } from './logbutton.directive';

describe('LogbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new LogbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
