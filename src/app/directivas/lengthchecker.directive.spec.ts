import { LengthcheckerDirective } from './lengthchecker.directive';

describe('LengthcheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new LengthcheckerDirective();
    expect(directive).toBeTruthy();
  });
});
